{{#> layout wedding}}
  
  {{> components/story-image }} 

  {{#each copy}}
    {{> components/story-copy }}
    {{#each choices}}
    {{> components/story-choices}}
    {{/each}}
  {{/each}}

{{/layout}}
