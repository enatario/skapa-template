<nav class="article-grid__nav nav">
  <p class="overline--min">Choose a climate story</a></p>
  <ul class="article-grid__nav-list nav__list">
    {{#each stories}}
    <li class="nav__item">
      <a href="/{{slug}}" class="nav__link" data-nav-link="{{slug}}">
        <div class="nav__icon">
          @@include("{{slug}}.svg")
        </div>
        <h3>{{title}}</h3>
        <p>{{tease}}</p>
      </a>
    </li>
    {{/each}}
  </ul>
</nav>
