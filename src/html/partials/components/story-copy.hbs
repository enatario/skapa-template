<section class="article-grid__content">
    {{#if landing}}
    {{> components/story-copy--header}}
    {{> components/story-copy--body}}
    {{> components/story-nav}}

    {{else}}
	{{> components/story-copy--header}}
    {{#each steps}}
    {{> components/story-choices}}
    {{#if @last}}
    {{> components/story-closer}}
    {{/if}}
    {{/each}}
    {{/if}}
  </section>
